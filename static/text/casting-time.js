// Time
var action = "Action";
var bonus = "Bonus";
var react = "Reaction";

function minute(time){
    return `${time} Min.`;
}

function hour(time){
    return `${time} Hr.`;
}

module.exports = {
    action,bonus,react,minute,hour,
}